mixin overlay(params = {})
  -
    const { createTagClassNames, loadStyle } = require("@utilities/pugComponents");
    const styles = loadStyle('Overlay');

    const { 
      id,
      className, 
      style, 
      attr, 

      data = {}, 
    } = params;

  div(
    id = id,
    class = [
      className,
      styles.element
    ],
    style = style
  )&attributes(attr)